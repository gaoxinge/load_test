from setuptools import find_packages, setup

setup(
    name="load_test",
    author="gaoxinge",
    version="0.0.3",
    description="load_test",
    url="https://github.com/gaoxinge/load_test",
    packages=find_packages(),
    install_requires=[],
    extras_require={},
    include_package_data=True,
)
